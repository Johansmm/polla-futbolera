# Admin secrets — never commit the real service account key
admin/serviceAccountKey.json

# Node
node_modules/
admin/node_modules/

# Lockfiles intentionally not committed — package.json pins exact versions
# instead, so `npm install` resolves the same tree without a large generated
# lockfile in the repo.
package-lock.json

# Firebase emulator local state
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/

# Personal Claude Code permission overrides — not shared with the team
.claude/settings.local.json
