{
  "name": "poya-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Test tooling for the Poya prediction pool — Firestore security rules tests, run against the local emulator. Not part of the deployed static site.",
  "type": "commonjs",
  "scripts": {
    "test": "firebase emulators:exec --only firestore \"node --test test/\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "3.0.4",
    "firebase-tools": "13.35.1"
  }
}
