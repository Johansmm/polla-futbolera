{
  "name": "polla-automation",
  "private": true,
  "version": "1.0.0",
  "description": "Admin-triggered scripts that need Firebase Admin SDK access, run via GitHub Actions (workflow_dispatch) — not intended to be run locally, though it can be for testing.",
  "type": "commonjs",
  "dependencies": {
    "firebase-admin": "12.7.0"
  }
}
